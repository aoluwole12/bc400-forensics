/**
 * Deprecated file.
 * Keep this only so older references don't break.
 * Use: src/indexer.ts
 */
console.log("⚠️  Deprecated: use src/indexer.ts (this file is intentionally inactive).");
process.exit(0);